// PermissionPilot — full onboarding wizard (depends on Core + UI).
//
// Scaffold stub. The first-run wizard (welcome → permissions → done), step
// indicator, theming, and persistence are implemented per
// docs/CLAUDE_CODE_BUILD_PROMPT.md after approval.

import SwiftUI
import PermissionPilotCore
import PermissionPilotUI

/// Namespace marker for the PermissionPilot module.
public enum PermissionPilot {}
