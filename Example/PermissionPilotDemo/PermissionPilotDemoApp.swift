// PermissionPilotDemo — example macOS app.
//
// Scaffold stub. Demonstrates the full wizard with 3–4 permissions once the
// flow is implemented per docs/CLAUDE_CODE_BUILD_PROMPT.md.

import SwiftUI
import PermissionPilot

@main
struct PermissionPilotDemoApp: App {
    var body: some Scene {
        WindowGroup {
            Text("PermissionPilot Demo — scaffold")
                .frame(width: 480, height: 320)
        }
    }
}
